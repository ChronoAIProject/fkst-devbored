name: devbored_contracts
description: Pure Dart contracts for the FKST devbored console.
version: 0.1.0
publish_to: none

environment:
  sdk: '>=3.0.0 <4.0.0'

dev_dependencies:
  lints: ^6.0.0
  test: ^1.25.0
